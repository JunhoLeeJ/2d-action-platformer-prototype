# ROADMAP — 스토리 콘텐츠 진행 상황 및 다음 작업 계획

이 문서는 사용자가 준 "인트로~2층 실구현" 원본 스펙(대화/컷신 시스템 + 1층 6구역 + 2층 4구역 +
결 동료 AI + 아트 제약)을 항목별로 쪼개서, 그중 무엇이 끝났고 무엇이 안 끝났는지, 다음엔 뭘 해야
하는지 추적하는 문서다. **PRD.md/SRS.md와 마찬가지로 게임이 바뀔 때마다 계속 업데이트해야 한다** -
특히 (a) 아래 체크리스트 항목 하나를 실제로 구현/완료했을 때, (b) 다음 작업 순서에 대한 판단이
바뀌었을 때, (c) 원본 스펙에 없던 새 인프라를 추가했을 때(예: 세로 카메라, floors/walls) 반영할 것.

세션이 자주 새로 시작되는 프로젝트라(사용자 메모 참고) 이 문서 하나만 읽어도 "지금 어디까지 됐고
다음에 뭘 해야 하는지" 알 수 있어야 한다 - 대화 기록에 의존하지 말 것.

## 지금 당장 다음에 할 일 (한 줄 요약)

**1층 구역 5(치명상 씬)** 부터. 구역 4(배경 몬스터)는 이번 세션에서 완료됨 - 아래 "1층 —
레벨 레이아웃" 표와 "다음 작업 순서 제안" 섹션 참고. 구역 5는 `reachingEntity`/`fragmentObject`,
"느린 걸음" 애니메이션(플레이스홀더, 이후 재사용 예정), 즉사 트리거, 화면 지지직 효과가 처음
필요한 구역이라 작업량이 있음.

---

## 1. 대화/컷신 시스템 — ✅ 완료 (파운데이션 세션)

원본 요구사항: 트리거 존(점프로 못 피함) + 이벤트 시퀀스(대사/카메라 홀드/애니메이션/페이드, 순서대로
재생) + 재생 중 조작 차단(대사 넘김 제외) + 텍스트박스(이름 있으면 표시, 지문("[정적]")도 가능) +
페이드가 다른 모든 것보다 우선 + 시퀀스가 확실히 끝나고 사라질 것(충돌 방지).

**구현 위치**: `js/engine/cutscene.js` (`startSequence`/`advanceSequence`/`endSequence`,
`dialogue`/`cameraHold`/`animation`/`fade`/`callback` 이벤트), `js/engine/zones.js`(트리거 존을
가진 존 그래프), `#cutsceneBox`/`#fadeOverlay`(index.html DOM 오버레이). 자세한 계약(각 이벤트
타입, 왜 `endSequence` 하나로 수렴하는지, `gameState==="cutscene"`이 왜 안전한지)은 `CLAUDE.md`의
"World model" 섹션에 이미 상세히 문서화되어 있음 - 여기서 중복 설명 안 함.

- ✅ 트리거 존 (walkIn/auto, X 범위만 판정 - 점프로 못 피함)
- ✅ 텍스트박스 (이름 선택 표시, 클릭/키로 다음)
- ✅ 카메라 홀드 (`cameraOverrideTarget`, x/y 각각 선택 가능)
- ✅ 애니메이션 이벤트 - **단, 실제 렌더링 훅만 있고 도형 기반 애니메이션 자체는 미구현** (§ 5 참고)
- ✅ 페이드 (암전/화이트아웃, DOM 오버레이라 HUD보다도 위)
- ✅ 시퀀스 하나로 수렴하는 종료 지점, 동시 실행 방지
- ✅ (파운데이션 이후 추가) `yMin`/`yMax`로 특정 층에서만 발동하는 트리거, `repeatable` 트리거의
  진입/이탈 판정(가만히 서있으면 매 프레임 재발동하던 버그 수정됨)

## 2. 1층 — 레벨 레이아웃

### 1층 전체 예외 규칙 (스펙 원문 그대로)
1. 표류는 구역 5 재방문까지 해금되지 않음 — ✅ `driftUnlocked=false`로 시작 (전역 플래그,
   `js/engine/zones.js`). ⚠️ **구역 5 재방문 시 `true`로 바꾸는 트리거는 아직 없음** (구역 5 자체가
   미구현이라 당연함).
2. NPC는 1층에 존재하지 않음 — ✅ `ruleFlags.hideGhostNpc=true`를 구역 1/2에 설정해 유령 동료를 숨김.
3. 구역 5 즉사 씬 제외 플레이어가 죽으면 안 됨(체력 닳는 건 보여야 함, 최소 체력 유지 + 무피격
   시간 경과 후 회복) — ✅ **구역 3에서 실제로 구현됨**: `CONFIG.HP_REGEN_DELAY`(마지막 피격 후
   회복까지 걸리는 무피격 시간) + `player.timeSinceHit` + `tickHpRegen()`(`js/entities/enemies.js`,
   플레이어/몬스터 공용) — 트리클(서서히 차오름)이 아니라 지연시간이 지나는 즉시 최대 체력으로
   스냅됨(사용자 피드백으로 변경). `ruleFlags.hpRegenDelay`가 있는 존에서만 발동하도록 게이트되어
   있어(구역 3에 설정됨) 2층 등 실제로 죽어야 하는 구간엔 영향 없음 — `hpFloor`(아래)와는 독립된
   레버라 존마다 "안 죽는다"/"회복된다"를 따로 켜고 끌 수 있고, 이후 난이도 조절에도 이 값만
   조정하면 됨. 이 회복은 몬스터에도 동일하게 적용됨(같은 `hpRegenDelay` 게이트, 구역 3의 포탑/체이서
   둘 다 대상). 몬스터 쪽은 죽음 자체는 리스폰 반복 대신 **개별 몬스터 단위 `hpFloor`**를 택함
   (`enemy.hpFloor`, `damageEnemy()` 공통 헬퍼가 근접 공격/표류 반격 양쪽에서 이 값을 존중) — 죽지
   않되 마지막 반 칸 핍에서 멈춰 "더 때려도 안 죽는다"가 보임. 구역 1/2는 애초에 피해를 줄 수단이
   없어서(적 없음, 낙사 없음) 이 인프라가 필요 없었을 뿐.

### 구역별 상태
| 구역 | 내용 | 상태 |
|---|---|---|
| 1. 진입부 | 튜토리얼: 이동, 점프 | ✅ 완료 (`f1z1_entry`) |
| 2. 플랫폼 형식 | 이동/점프 연습, 낙사 없음 | ✅ 완료, 세 번 재설계됨 (`f1z2_platforms` — 1차: 존 전체 폭 기준 느슨한 지그재그 → "너무 노가다스럽다"는 피드백으로 좁은 통로(샤프트) 형태로 축소. 2차: 샤프트가 "계단 꼭대기의 보너스"였고 문은 땅바닥에 있던 걸, 샤프트를 통과 필수 경로로 바꾸고 문을 샤프트 꼭대기 착지대로 옮김. 3차: 문에 Y 범위 제한이 없어서 땅바닥으로 그냥 걸어가도 발동하던 버그를 `yMax`로 수정, 존 꼭대기에 천장 추가(싱글 점프 통과/더블 점프 헤딩), 문에 공중으로 진입 시 즉시 암전 대신 제자리 낙하→오른쪽으로 걸어 화면 밖으로 사라짐→암전으로 이어지는 연출 추가 — § 6 참고) |
| 3. 근접 공격 연습 | 포탑 1 + 체이서 1, 죽지 않되 체력 깎이는 건 보임(리스폰 반복 또는 불사 중 택1 — 스펙에서 둘 다 허용) | ✅ 완료 (`f1z3_melee_practice`, `js/levels/f1z3-melee-practice.js`) — 불사 방식(개별 몬스터 `hpFloor`) 택함 |
| 4. 배경 몬스터 | 적 A 2마리 + 적 B 1마리, 전투 대상 아님(지나가는 구간) | ✅ 완료, 사용자 피드백으로 재조정됨 (`f1z4_background_monsters`, `js/levels/f1z4-background-monsters.js`) — 적 A(mimeA)는 실제 `enemies[]` 몬스터(체이서 재사용)로 남고 **봉쇄 벽(`wallGates`)이 이 둘을 다 잡아야 열리게 추가됨**(원안의 "전투 대상 아님"에서 방향 전환). 적 B(mimeB)는 반대로 "적"이라는 정의 자체를 없애고 `zone.ambientProps`(CLAUDE.md 참고)로 옮김 - 체력 UI 없음, 공격 판정 자체가 없어 구조적으로 죽을 수 없고, `enemies[]`에 없으므로 봉쇄 벽 조건과도 완전히 무관함(별도 예외 처리 불필요). 인트로 대사는 뺌(§ 아래) |
| 5. 치명상 씬 | 막다른 방, `reachingEntity`+`fragmentObject`, 접근 시 느린 걸음→화면 지지직→즉사→사망씬→구역1로 텔레포트+페이드인 | ❌ 미착수 — "느린 걸음" 애니메이션(향후 재사용 예정)도 아직 플레이스홀더조차 없음 |
| 6. 표류 연습 | 구역 3과 비슷하지만 배치 다르게, 접근성 더 어렵게 | ❌ 미착수 |
| 구역 1 재진입 처리 | 문에 "새 균열" 스프라이트 오버레이, 구역 5 재도달 시 `reachingEntity` 제거+`crackMark`만 남음, V키로 표류 해금(짧은 컷신) | ❌ 미착수 |
| 층 이동 (1층→2층) | 구역 6 끝 문 = 층 이동 트리거존, 암전 후 2층 구역 1로(컷신 계속 이어짐) | ❌ 미착수 |

### 구조 관련 확정 사항 (재확인용)
- 존 하나 = 맵 하나. 오른쪽 끝 문(트리거 있음)으로만 다음 구역 이동, 왼쪽 문은 배경(트리거 없음,
  뒤로 못 감). 층 이동도 같은 방식(암전 컷신) — 이 전부 **엔진 차원에서 이미 범용으로 구현됨**
  (`doors.right`→자동 트리거 합성, `js/engine/zones.js`의 `makeDoorTrigger`). 새 구역/층을 만들 때
  추가 엔진 작업 없이 zone def의 `doors`만 채우면 됨.

## 3. 2층 — 레벨 레이아웃

원본 스펙에 "3개 구역"이라 써있지만 실제로는 4개 항목(결 조우/체크포인트/몬스터들/보스방)이
나열되어 있음 — 원문 오타로 보고 4구역으로 취급.

| 구역 | 내용 | 상태 |
|---|---|---|
| 1. 결과 조우 | 결(gyeol) NPC, 몬스터 3마리에 둘러싸임. 1층 컷신에서 이어지는 대사 4줄(아래 원문 보존) 후 결을 AI 동료로 전환 | ❌ 미착수 |
| 2. 체크포인트 | 매듭 묶인 기둥. 손 갖다대는 애니메이션→매듭 파랗게 반짝→은은한 빛 유지. 이후 모든 구역 왼쪽 문 옆엔 체크포인트 기둥(첫 체크포인트 제외, 컷신 없이 자동 활성화) | ❌ 미착수 — 엔진(`activateCheckpoint`)은 파운데이션 때 이미 만들어뒀음, 콘텐츠(기둥 오브젝트+트리거+반짝임 연출)만 없음 |
| 3. 몬스터들 | 기존에 만들어놨던 맵(전투 프로토타입) 재활용 | ⚠️ 존 자체는 존재(`f2z3_legacy_arena`)하지만 **어느 문에서도 연결 안 됨** — 2층 구역 2의 문이 여기로 이어지게 배선만 하면 됨(콘텐츠 새로 안 만들어도 됨) |
| 4. 보스방 "지키는 자" | 화면 크기=맵 크기(카메라 고정), `keeperBoss`(크고 스턴면역·공속빠름·체력많은 포탑 변형), 1/2/3페이즈(색+공속 변화, 컷신 삽입 가능), 처치 시 컷신(느려짐→문 쪽으로 돌아섬→`releaseSpot`으로 플레이어 이동→보스 파티클로 흩어짐)+대사, 이후 1층 구역 1로 페이드아웃(재시작 가능하게 만들 필요는 아직 없음) | ❌ 미착수 |

### 2층 구역 1 대사 원문 (그대로 보존 — 나중에 구현할 때 재해석하지 말고 이 문구 사용)
1. "와, 고마워! 이름이 뭐야?" (화자: `???`, 아직 미정)
2. (짧은 카메라 홀드, 결 표정 클로즈업 — 지금 단계에선 확대 정도로만 처리 가능)
3. "...말 안 해도 상관없어! 나 결이야." (화자: `결` — 이후 별도 언급 없으면 화자는 항상 결)
4. "...너도." (결, 주인공 쪽 잠깐 바라보는 모션)

### 2층 구역 4 보스전 완결 대사 원문
"...저렇게, 그냥 놓아줄 수도 있는 거구나."

## 4. 결(gyeol) — AI 동료 — ❌ 미착수

- **일반 게임플레이 중**: 플레이어를 따라다니는 AI 동료. 기존에 만들어둔 유령 NPC(`ghostNpc`,
  현재는 100% 순수 연출 — `js/entities/player.js`)를 재활용해서 2층부터 실제 동료로 전환할 예정.
  랜덤 간격으로 짧은 바크 대사(게임플레이 영향 없음, 자동으로 넘어감, 말풍선 등 컷신 텍스트박스와는
  다른 형식이어도 됨) — 예시 대사(스펙 원문, 나중에 확장 예정): "저거 왼쪽으로 돌아가면 안 맞을 것
  같은데!" / "너 진짜 말 없다... 원래 이래?"
- **컷신성 이동 구간**: 특정 트리거 존에서 "결이 앞서 걷고 대사 재생"하는 별도 모드로 전환, 끝나면
  일반 동료 AI로 복귀.
- 지금 `ghostNpc`는 CLAUDE.md에 명시된 대로 "어떤 충돌/전투 판정에도 관여하지 않음"이 불변 조건 —
  결로 전환할 때 이 순수-연출 특성을 유지할지, 아니면 실제 상호작용(예: 전투 보조)을 넣을지는
  아직 스펙에 없음 → 그 단계에서 사용자에게 확인 필요.

## 5. 아트 제약 — 도형 기반 애니메이션

실제 그림 에셋 없이 단순 도형의 이동/회전/펄스만으로 표현하라는 원칙(스펙 원문):
- ✅ **적 A** (손짓 반복): 몸통 사각형 + 팔(작은 사각형/막대)이 일정 각도로 왔다갔다 회전. 감지 전엔
  반복, 감지 후엔 멈추고 전투 이동으로 전환. — 구현됨(`makeMimeA`, `js/entities/enemies.js` +
  렌더링 `js/rendering.js`). **판단 사항**: "전투 이동"을 문자 그대로 받아들여 mimeA는 실제 전투
  능력(감지→추적→근접 공격)을 체이서(`makeChaser`)에서 그대로 재사용한다(오브젝트를 만든 뒤 type/체력만
  덮어씀 - `updateChaserAI`는 타입을 내부에서 검사하지 않으므로 완전히 동일하게 동작함). "배경 몬스터"
  라는 성격(구역 4에서 강제 전투가 아님)은 AI를 약화시켜서가 아니라 그 구역에 `wallGates`를 안 둬서
  순수 레벨 디자인으로 만듦 - 새 전투 로직을 처음부터 짤 필요가 없어서 이 방식을 택함. 유휴 손짓
  애니메이션은 `enemy.aiState`(patrol/return = 흔들림, chase/windup/recovery = 정지)를 그대로
  참고해서 그리므로 별도 애니메이션 상태 필드가 필요 없었음. 팔은 항상 몸통 오른쪽에 고정해서 그림
  (facing에 따라 반전 안 함 - 플레이스홀더라 의도적으로 생략). **이후 사용자 피드백으로 방향 전환**:
  구역 4에 실제 봉쇄 벽(`wallGates`)을 추가해서 이제 mimeA 2마리를 다 잡아야 다음으로 진행 가능 -
  "전투 대상 아님"이라는 원안 문구는 이제 mimeA가 아니라 적 B(아래)에만 해당됨.
- ✅ **적 B** (웅얼거림): 시각적 동작 없음, 몸통 색이 아주 옅게 주기적으로 밝아졌다 어두워지는 pulse.
  — 구현됨(`drawMimeBProp`, `js/rendering.js`). **판단 사항(사용자 확인)**: 처음엔 mimeA와 마찬가지로
  `enemies[]` 소속 몬스터(체력/스턴 있음, 그냥 AI만 없음)로 만들었으나, 사용자가 "말 그대로 배경에만
  있는 애라 체력 UI도 없애고 죽지도 못하게, '적'이라고 정의하지 않아도 된다"고 명확히 해서 완전히
  `enemies[]` 밖으로 빼고 `zone.ambientProps`(CLAUDE.md 참고)로 재구현함 - 공격 판정 자체를 안 거치므로
  "안 죽음"이 무적 플래그가 아니라 구조적으로 보장되고, 체력/HP pip도 애초에 없음. 이 설계 덕분에
  "봉쇄 벽 조건에서 적 B의 생사는 무관해야 한다"는 요구사항도 별도 예외 처리 없이 저절로 만족됨
  (`isGateLocked`가 보는 `enemies[]`에 애초에 없으므로). pulse는 별도 타이머 필드 없이
  `performance.now()`(개체마다 x좌표로 위상만 다르게)를 렌더링에서 직접 읽어서 그림 - 기존 포탑
  발사 예고 pulse와 같은 패턴 재사용.
- ❌ **`reachingEntity`** (손 뻗은 형체, 구역 5): 몸통+팔 막대가 반복 아닌 한 방향으로 뻗은 채 고정된
  정지 자세. 애니메이션 없음. — 아직 미착수(다음 작업).
- ❌ **"느린 걸음" 애니메이션**: 구역 5, 보스전 등 여러 컷신에서 재사용될 예정 — 플레이스홀더로라도
  하나 만들어두라고 스펙에 명시됨. — 아직 미착수.
- **결**: 지금은 그냥 기존 npc 정도로 충분, 표정/세부 동작 시도하지 말 것(스펙 원문). — 아직 결 자체가 미착수.

적 A(mimeA)는 기존 `ENEMY_FACTORIES` 패턴을 그대로 따름(CLAUDE.md "Every enemy carries generic
detection/leash/attack-range instance fields" 참고) - `damageEnemy`의 스턴 리셋 분기와
`updateEnemies`의 AI 디스패치 분기도 이 타입에 맞게 확장함(§ 6 참고). 적 B(mimeB)는 반대로
`ENEMY_FACTORIES`/`enemies[]`에 전혀 없다 - `zone.ambientProps`에 데이터만 있고 `draw()`가 직접
그린다(§ 6, CLAUDE.md 참고). 다음에 새 몬스터/장식이 필요하면: 실제로 죽거나 공격받아야 하면
mimeA처럼 `enemies[]`에(기존 AI 재사용 가능하면 재스킨), 순수 배경이면 mimeB처럼 `ambientProps`에
넣을 것 - 어느 쪽이든 "감지/공격을 하지만 안 죽어야 함"은 `hpFloor`(구역 3 패턴)로, "아예 판정 자체가
없어야 함"은 `ambientProps`로 - 이 둘을 헷갈리지 말 것.

**대사 관련 메모**: 구역 3/4에 세션 중 임시로 넣었던 인트로 대사 두 건은 사용자 요청으로 전부 뺐음
(현재 두 존 다 `triggerZones: []`) - 컷신 엔진(`js/engine/cutscene.js`) 자체는 그대로 있고, 사용자가
정확한 문구/타이밍을 직접 지정할 예정이라 그때 다시 채워 넣을 것. **앞으로 레벨에 대사를 임의로
지어 넣지 말 것** - 사용자가 명시적으로 지정하기 전까지는 트리거 존에 dialogue 이벤트를 추가하지 않는다.

## 6. 파운데이션 이후 추가된 엔진 기능 (원본 스펙엔 없었지만 지금 존재함)

아래는 1층 구역 1/2를 만들면서 사용자 피드백으로 새로 생긴 범용 인프라 — 앞으로 구역 3~6, 2층
전체를 만들 때 재사용할 것. 상세 계약은 `CLAUDE.md`에 이미 문서화되어 있어 여기선 목록만:

- 세로 카메라 스크롤 (`CAMERA_SMOOTHING_X/Y`, `zone.height`, `cameraBounds.minY/maxY`)
- `zone.floors`/`zone.walls` — 기본 바닥 외 임의 위치(부분 구간 `xMin/xMax`·`yMin/yMax` 가능)에
  놓는 바닥/벽. 강제 지그재그 등 레벨 디자인에 사용
- 존 좌우 끝 벽 + 왼쪽 문 40px 안쪽 배치 관례 (모든 새 존에 적용할 것)
- 트리거 존 `yMin`/`yMax` (다층 존에서 특정 층에서만 발동하게 하는 비대칭 여유)
- `wallGates`(봉쇄 벽)가 X 범위만 판정하도록 일반화됨 — 세로로 긴 존에서도 안전
- (구역 3에서 추가, 이후 일회성이 아니라 범용으로 재설계됨) 무피격 시간 경과 후 즉시(스냅) 최대
  체력 회복 — `CONFIG.HP_REGEN_DELAY` + `{hp,maxHp,timeSinceHit}` 최소 인터페이스 + `tickHpRegen()`
  (`js/entities/enemies.js`, 플레이어/몬스터 공용 - `updatePlayer`와 `updateEnemies` 둘 다 이 함수
  하나를 호출). `ruleFlags.hpRegenDelay`(숫자, `hpFloor`와는 독립된 별개의 존 규칙)가 걸린 존에서만
  발동 — 없는 존(2층 등)엔 자동으로 무영향. 새 존에서 켜려면 `ruleFlags.hpRegenDelay`에 값만 넣으면
  플레이어와 그 존의 모든 몬스터에 동시에 적용됨(개별 on/off 아님). 난이도 조절(값 조정/`null`로 끄기)을
  염두에 두고 설계됨 - `hpFloor`(죽지 않음)와 `hpRegenDelay`(회복됨)는 항상 같이 쓸 필요 없는 독립된 레버.
- (구역 3에서 추가) 몬스터 개별 `hpFloor` + 공용 `damageEnemy(enemy, amount)` 헬퍼
  (`js/entities/enemies.js`) — 근접 공격/표류 반격 두 곳에 중복돼 있던 "데미지 적용+스턴+사망 판정"을
  하나로 합치면서 hpFloor 분기를 추가함. `opts.hpFloor`를 몬스터 스폰 시 넣으면 그 수치 이하로 안
  죽는 "연습용" 개체가 됨(HP 핍은 정직하게 줄어들어 보이되 0에서 안 멈추고 hpFloor에서 멈춤) — 죽여야
  하는 몬스터는 그냥 opts에서 생략(기본값 null)하면 기존 동작 그대로.
- (구역 2 재재설계 세션에서 추가) **천장** — 세로로 긴 존은 앞으로 꼭대기에 `zone.floors`로 천장을
  하나 둘 것(바닥 관례의 대칭). 헤드룸(서 있는 높이~천장 밑면)은 실제 프레임 단위 점프 물리를
  시뮬레이션해서 싱글 점프는 통과, 더블 점프(정점에서 재점프)는 막히게 잡는다 — `f1z2_platforms`가
  실측 예시(싱글~131px vs 더블~275px, 헤드룸 200px). 상세는 CLAUDE.md 참고.
- (구역 2 재재설계 세션에서 추가) **문의 `yMin`/`yMax`/`landingY`** — 문이 땅바닥이 아니라 높은
  착지대 위에 있을 때, 그 아래 땅바닥을 그냥 걸어가도 발동해버리는 걸 막는 Y 범위 제한(`yMax` 타이트,
  `yMin`은 점프 진입 허용을 위해 생략). `landingY`가 있으면 공중(`!player.onGround`)에서 트리거가
  발동했을 때 즉시 암전 대신 제자리 낙하→오른쪽으로 걸어 화면 밖으로 사라짐→암전 순서로 이어붙임
  (`js/engine/zones.js`의 `makeDoorTrigger`/`makeFallInPlaceTick`/`walkRightOffscreenTick`).
- (같은 세션에서 추가) **컷신 `"custom"` 이벤트 타입** + **함수형 `trigger.sequence`**
  (`js/engine/cutscene.js`) — `custom({tick,onStart})`은 고정 시간이 아니라 "조건이 될 때까지"
  매 프레임 `tick(dt)`를 돌리는 이벤트(위 문 착지 연출이 이걸로 만들어짐). `trigger.sequence`를
  배열 대신 함수(`() => events[]`)로 주면 트리거가 실제로 발동하는 순간에 평가되어, 존 로드 시점엔
  알 수 없는 런타임 상태(예: 발동 시점에 플레이어가 공중이었는지)에 따라 다른 이벤트 목록을 고를 수
  있음 — 앞으로 "상황에 따라 다른 컷신이 필요한" 트리거에 재사용할 것.
- (구역 2를 몇 번 더 플레이해보며 발견된 버그 수정 세션에서 추가) 위 세 항목을 실제로 써보니 나온
  문제 세 가지, 전부 수정 완료 — 앞으로 같은 패턴 쓸 때 반드시 참고할 것:
  - **`yMax`는 "땅바닥만 제외"가 아니라 "그 바닥에 서 있을 때 높이 근처"로 타이트하게.** 처음에
    `yMax:900`으로 잡았더니, 샤프트 근처에서 점프를 섞어 오르내리다가 착지대보다 한참 아래(하지만
    900보다는 위)에서도 문이 발동해버리는 버그가 있었음 — `landingY - player.h + 25` 처럼 실제 서
    있는 높이에서 딱 25px 정도만 여유를 두는 식으로 고침(`f1z2_platforms`가 실제 예시).
  - **컷신 중 스크립트 낙하(fall-in-place)가 천장 충돌을 안 봐서 뚫고 올라가는 버그.** 트리거 발동
    직전 이단 점프 관성으로 아직 상승 중이었으면, `updatePlayer`가 멈춘 동안(cutscene) 그 상승이
    충돌 판정 없이 그대로 이어져 천장을 뚫었음 — `updatePlayer`의 solidPlatforms Y축 충돌 판정을
    `resolveSolidVerticalCollisions()`로 뽑아내 양쪽이 공유하도록 고침(`js/entities/player.js`).
  - **문/entryPoint/checkpoint의 y를 매직 넘버로 눈대중 잡았던 것 전부 수정.** 문의 `y+h`가 바닥
    surface보다 몇 px 더 내려가 있어서 시각적으로 땅에 박혀 보였고, entryPoint/checkpoint도 서 있는
    높이보다 위쪽이라 존 진입/게임 시작 직후 짧게 공중에서 낙하하는 게 보였음 - 이제 모든 존 파일이
    `standingTopY = floorY - player.h` 하나만 계산해서 entryPoint/checkpoint/문 y를 전부 그걸로부터
    도출함(f1z1/f1z2/f1z3/f2z3 전부 적용됨) - 새 존 만들 때도 이 패턴을 그대로 따를 것.
  - **(추가 수정)** `landingY`(공중 진입 시 제자리 낙하->오른쪽으로 걸어나가기->암전 연출)를 처음엔
    "착지대가 따로 있는 존만 해당"으로 좁게 잡았는데, 사용자가 명시적으로 **모든 문에 공통 적용**
    하라고 확인해줌 - `f1z1_entry`처럼 평범한 땅바닥 문도 `landingY: groundY`를 넣어서, 점프해서
    문에 들어가면 땅바닥 존이어도 똑같이 낙하->화면 밖으로 걸어나가기->암전 연출이 나가게 고침.
    앞으로 만드는 문은 전부(땅바닥이든 착지대든) `yMax`+`landingY` 둘 다 기본으로 넣을 것(CLAUDE.md
    참고) - "착지대가 있는 특수한 경우에만 필요"라고 되돌아가지 말 것.
- (구역 4 세션에서 추가) **"재스킨" 몬스터 타입 패턴** — 새 몬스터가 시각적으로만 다르고 전투 로직은
  기존 타입과 같아도 되면(mimeA/체이서처럼), 팩토리를 처음부터 새로 짜지 않고 기존 팩토리를 호출한
  뒤 스프레드로 복사하고 `type`/체력 등 진짜 다른 필드만 덮어쓴다(`makeMimeA`가 `makeChaser`를 이렇게
  재사용 - `js/entities/enemies.js`). `updateChaserAI`/`updateTurretAI`처럼 내부에서 `enemy.type`을
  안 보고 인스턴스 필드만 읽는 함수는 재스킨된 타입에도 그대로 재사용 가능 - 다만 함수 **밖에서**
  `enemy.type === "chaser"`로 분기하던 자리(`damageEnemy`의 스턴 리셋, `updateEnemies`의 AI 디스패치,
  `rendering.js`의 색/판정 미리보기 분기)는 전부 새 타입도 같이 타도록 조건을 넓혀야 한다는 게
  놓치기 쉬운 부분 - `isChaserLike`처럼 합쳐진 플래그를 만들어 참고할 것.
- (구역 4 세션에서 추가, 이후 같은 세션에서 재설계됨) **`zone.ambientProps` — enemies[] 밖의 순수
  배경 장식** (CLAUDE.md World model 섹션에 계약 문서화됨). 처음엔 mimeB를 "완전 수동형 몬스터"로
  `enemies[]` 안에 넣었었는데(감지/공격 없이 그냥 서있기만 하는 타입), 사용자가 "적이라고 정의하지
  않아도 된다, 체력 UI도 없이, 죽지도 못하게"라고 명확히 해서 아예 `enemies[]`/`ENEMY_FACTORIES` 밖으로
  뺌 - `draw()`가 `currentZone.ambientProps`를 따로 순회하며 그리기만 하고, `damageEnemy`/`wallGates`
  등 어떤 전투 코드도 이 배열을 보지 않는다. 새로 뭔가를 추가할 때 판단 기준: 공격을 받거나(맞아서
  플래시 표시 등) 좌표/hitbox 판정에 걸려야 하면 `enemies[]`(안 죽어야 하면 `hpFloor` 병행), 순수
  시각 효과일 뿐 어떤 판정에도 안 걸려야 하면 `ambientProps` - 후자를 `enemies[]`에 넣고 무적 처리로
  때우려 하지 말 것(이번에 실제로 겪은 되돌림).
- (구역 4 세션에서 추가) **도형 애니메이션은 저장 상태 없이 `performance.now()`에서 직접 유도** —
  포탑의 발사 예고 pulse가 이미 쓰던 패턴(`Math.sin(performance.now()/N + 위상오프셋)`)을 몬스터/장식
  유휴 애니메이션(mimeA 팔 흔들기, mimeB 몸통 pulse)에도 그대로 재사용함 - `dt` 기반으로 매 프레임
  갱신하는 별도 타이머 필드가 필요 없고, 위상 오프셋을(`enemies[]` 소속이면 `enemy.id`, `ambientProps`
  처럼 id 개념이 없으면 `prop.x` 같은 좌표값을) 섞으면 같은 타입 여러 개가 화면에 있어도 서로 안
  겹치게(싱크 안 맞게) 저절로 흩어진다. 향후 도형 애니메이션(느린 걸음, reachingEntity 등)도 새 타이머
  필드부터 만들지 말고 이 패턴을 먼저 검토할 것.
- (구역 4 세션 이후 추가) **QA 패널 — 구역 이동(`js/engine/qapanel.js`, 백틱 키로 토글)**. 사용자
  요청으로 추가: 지금은 QA/레벨 확인용이지만 **게임을 전부 클리어한 플레이어에게도 그대로 줄 정식
  기능**이라서 디버그 임시 코드가 아니라 제대로 정리 로직을 갖춘 상시 기능으로 만들었음(CLAUDE.md에
  계약 상세 문서화됨) - `warpToZone()`이 컷신/페이드/타임스톱 상태를 강제로 정리하고, `respawnPlayer`와
  동일하게 생존 상태(HP/쿨다운)를 리셋하고, `currentCheckpoint`도 워프한 존으로 재설정한다(안 그러면
  워프 직후 죽었을 때 엉뚱한 존으로 되돌아감). 이때 처음으로 **`zone.name`** 필드(사람이 읽을 표시용
  이름)를 모든 존에 추가함 - 이후 새 존을 만들 때도 `entryPoint`/`checkpoints`처럼 항상 채울 것(QA
  패널과 향후 HUD 존 이름 표시가 이 필드를 읽음, 없으면 raw id로 폴백). 패널이 열려있는 동안은
  `timeStopTimer`와 같은 방식으로 `loop()`가 `update()`를 건너뛰어 게임이 완전히 멈춘다.

## 7. 다음 작업 순서 제안

원본 스펙 순서(1층 먼저)를 그대로 따르되, 구역별 의존성 고려:

1. ~~**1층 구역 3** (근접 공격 연습)~~ ✅ 완료 — HP 최소값+자연 회복, 몬스터 개별 `hpFloor`(불사
   방식) 둘 다 구현됨. 자세한 내용은 § 2 구역별 상태 표 + § 6 참고.
2. ~~**1층 구역 4** (배경 몬스터)~~ ✅ 완료 — 적 A(mimeA, 체이서 재스킨 - 실제 `enemies[]` 몬스터,
   봉쇄 벽으로 다 잡아야 진행)와 적 B(mimeB, `enemies[]` 밖의 순수 배경 장식 `ambientProps` - 체력 UI
   없음, 죽을 수 없음) + 도형 애니메이션(팔 흔들기/pulse) 전부 구현됨. 인트로 대사는 사용자 요청으로
   뺌(대사 시스템 자체는 유지, 나중에 사용자가 직접 문구 지정 예정). 자세한 내용은 § 2 구역별 상태 표
   + § 5 + § 6 참고.
3. **1층 구역 5** (치명상 씬) — 다음 차례. `reachingEntity`/`fragmentObject`, 느린 걸음 애니메이션(플레이스홀더,
   이후 재사용), 즉사 트리거, 사망씬 플레이스홀더, 화면 지지직 효과.
4. **1층 구역 1 재진입 처리** — 균열 오버레이, `crackMark`, V키 표류 해금 트리거.
5. **1층 구역 6** (표류 연습) — 구역 3 재활용 + 배치 변경. hpFloor+회복 인프라는 구역 3에서 이미
   범용으로 만들어졌으므로 `ruleFlags.hpFloor`만 채우면 그대로 재사용 가능.
6. **1층→2층 전환 컷신 + 2층 구역 1** (결 조우, 결의 AI 동료 전환).
7. **2층 구역 2** (체크포인트 콘텐츠 — 엔진은 이미 있음).
8. **2층 구역 3** (기존 `f2z3_legacy_arena` 배선만 하면 됨 — 콘텐츠 작업 거의 없음, 순서상 당겨도 무방).
9. **2층 구역 4** (보스전).
10. 결 동료의 "일반 게임플레이 중 바크 대사" 시스템은 결이 실제로 등장하는 2층 구역 1 작업과 함께.

각 구역을 시작하기 전에 이 문서를 다시 훑고, 완료되면 위 표의 상태를 갱신할 것.

## 8. 판단이 필요한 미해결 사항 (해당 구역 작업 시 사용자에게 확인)

- ~~구역 3/6 몬스터: 리스폰 반복 vs 불사~~ 구역 3에서 **불사**로 결정(개별 몬스터 `hpFloor`) —
  리스폰 반복은 체크포인트/타이머 등 부가 상태가 더 필요해서 복잡도 대비 얻는 게 적다고 판단함.
  구역 6도 특별한 이유가 없다면 같은 방식을 재사용할 것.
- 결 동료의 판정 관여 여부: 순수 연출(현재 유령 NPC와 동일) vs 실제 전투 보조
- 사망씬/치명상씬의 구체적 연출(사운드 포함)은 스펙상 "지금은 플레이스홀더"로 명시되어 있어
  나중에 다시 다룰 예정 — 지금 단계에서 과하게 다듬지 말 것
